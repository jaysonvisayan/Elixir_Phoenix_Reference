defmodule Innerpeace.Db.DataTables.PackageDataTablesTest do

   # use Innerpeace.PayorLink.Web.ConnCase

  # setup do
      # package = insert(:package, code: "testName", name: "testName")
      # benefit = insert(:benefit, code: "123456", name: "sample")
      # # benefit_package = insert(:benefit_package, benefit_id: "123456", package_id: "224466")

      # _package2 = insert(:package, code: "testName2", name: "testName2")
      # _benefit2 = insert(:benefit, code: "78902", name: "sample2")
      # # benefit_package2 = insert(:benefit_package, benefit_id: "789024", package_id: "224467")

      # _package3 = insert(:package, code: "testName3", name: "testName3")
      # _benefit3 = insert(:benefit, code: "78903", name: "sample3")
      # # benefit_package3 = insert(:benefit_package, benefit_id: "789035", package_id: "224468")

      # user = fixture(:user_permission, %{keyword: "manage_accounts", module: "Products"})
      # conn = authenticated(conn, user)
      # {:ok, %{conn: conn, package: package, benefit: benefit}}

    # end

    # test "validate data in datatable view packages", %{conn: conn, package: package}  do


    # end

end
